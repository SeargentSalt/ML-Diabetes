import React from 'react'
import Button from 'react-bootstrap/Button';
import Jumbotron from 'react-bootstrap/Jumbotron';
export default function Instructions () {
  return (

 <Jumbotron>
   <h1>Instructions on how to use </h1>
   <p>
   To use the web app simply select a file and upload to the cloud. It will then be run on our machine learning model which will then send back the result of if the image we saw was from a person suffering from Diabetic Retinopathy.

   </p>
 </Jumbotron>
)
}
